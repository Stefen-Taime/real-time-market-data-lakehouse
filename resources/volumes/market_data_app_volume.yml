resources:
  volumes:
    market_data_app_volume:
      catalog_name: workspace
      schema_name: default
      name: ${var.app_volume_name}
      volume_type: MANAGED
      comment: App-facing Unity Catalog volume for Streamlit market observability assets.
